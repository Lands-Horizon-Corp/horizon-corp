root = "."
tmp_dir = "tmp"

[build]
bin = "tmp/main"
cmd = "cd cmd/server && swag init && cd ../../ && go build -o tmp/main cmd/server/main.go"
delay = 1000
exclude_dir = ["assets", "tmp", "vendor", "cmd/server/docs", "json", "yaml"]
include_ext = ["go", "tpl", "tmpl", "html"]

[log]
time = true
